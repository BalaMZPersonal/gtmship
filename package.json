{
  "name": "gtmship",
  "private": true,
  "description": "Build GTM workflows with AI. Ship to your cloud.",
  "license": "MIT",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo build",
    "build:release": "node scripts/build-homebrew-release.mjs",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "render:homebrew-formula": "node scripts/render-homebrew-formula.mjs",
    "render:homebrew-update-manifest": "node scripts/render-homebrew-update-manifest.mjs",
    "clean": "turbo clean",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  }
}
